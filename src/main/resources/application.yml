spring:
  mail:
    host: smtp.gmail.com
    port: 587
    username: comliraz@gmail.com
    password: vzeodcjlhwbjrusf
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/class_management?useTimezone=true&serverTimezone=UTC
    username: root
    password:
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
      show-sql: true

  main:
    allow-bean-definition-overriding: true